function y = outputFcnPendulum(x, ~, ~)
% Output function for pendulum system
% Inputs:
%   x: State vector [theta (rad); dtheta (rad/s)]
% Output:
%   y: Output variable (here, the pendulum angle theta)

y = x(1);

end

